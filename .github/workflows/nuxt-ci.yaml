name: nuxt-ci

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"] 

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:

      - name: Atualiza ubuntu
        run: sudo apt-get update && sudo apt-get upgrade -y

      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '18'

      - name: Checkout code
        uses: actions/checkout@v5


      #- name: Clean install
      #  run: |
      #    rm -rf node_modules package-lock.json
      #    npm install


      #- name: Run build
      # run: npm rebuild